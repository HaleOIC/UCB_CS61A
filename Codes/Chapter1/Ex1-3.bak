#lang sicp
(define (Sele-two-of-three a b c )
  (define (min a b c)
    (define ans a)
    (if (< b ans)
        (define ans b))
    (if (< c ans)
        (define ans c))
             
        
  
  )